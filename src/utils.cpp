#include "globals.h"

void GetClientRect(sk::pose_t pose, sk::bounds_t bounds, RECTX* rect) {
	//rect->left = pose.x;
	//rect->right = pose.x + bounds.x;
	//rect->top = pose.y;
	//rect->bottom = pose.y + bounds.y;
}
